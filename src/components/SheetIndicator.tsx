import { Text, View } from 'react-native'

export default function SheetIndicator() {
  return (
    <View className='w-full bg-gray-300 dark:bg-gray-600 rounded-full my-2 mx-auto' style={{width: 50, height: 4}} />
  )
}
